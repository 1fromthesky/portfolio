.connect {
    
   &__title {
      font-weight: 700;
      font-size: 36px;
      line-height: 1;
      @include tablets {
         text-align: center;
      }
      @include phones {
         font-size: 24px;
      }
   }
   &__form {
      margin-top: 110px;
      @include phones {
         margin-top: 40px;
      }
   }
}