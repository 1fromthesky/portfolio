.social {
   display: flex;
   align-items: center;
   
   &__item {
      margin-right: 40Px;
      
      &:last-child {
         margin-right: 0;
      }
   }

   &__link {
      width: 25Px;
      height: 25Px;

      &:hover .social__svg {
         fill: $main;
      }
   }

   &__svg {
      fill: currentColor;
   }
}

.header__social>.social {
   @include phones {
      display: none;
   }
}