.nav {
   
   @include tablets {
      display: none;
   }
   
   &__list {
      // width: 100%;
      display: flex;
      align-items: center;
      // justify-content: center;
   }

   &__item {
      margin-right: 30px;

      &:last-child {
         margin-right: 0;
      }
   }

   &__link {
      // padding: 5px 5px;
      white-space: nowrap;
   }
}
