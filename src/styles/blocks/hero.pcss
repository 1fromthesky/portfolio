.hero {
   height: 100vh;
   min-height: 650px;

   &__content {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
   }

   &__scroll-btn {
      position: absolute;
      bottom: 0px;
      left: 50%;
      transform: translate(-50%, -50%);
   }
}